# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/

# Site settings
title: pia-in-cebu.de
email: piasreise@gmx.de
instagram: pia.rtt
description: >-
  Hi, ich bin Pia. Für 11 Monate lebe ich auf den Philippinen und engagiere mich im IJFD bei Christ for Asia. Folge mir auf meiner Reise!
baseurl: ""                     # the subpath of your site, e.g. /blog
url: "https://pia-in-cebu.de"   # the base hostname & protocol for your site, e.g. http://example.com
lang: de

# Build settings
theme: minima
markdown: kramdown
highlighter: rouge

# Plugins
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Collections
collections:
  pages:
    output: true
    permalink: /:slug/
  posts:
    output: true

# Defaults
defaults:
  - scope: { path: "" }
    values:
      layout: "default"
      lang: "de"

# Sass/Assets
sass:
  style: compressed
